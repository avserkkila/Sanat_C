#define MAXLEN_STDIN 200
#define N_SUOTTEITA_KIERROKSELLA 100
#define TOISTA(mihin) for(int tmp=0; tmp<(mihin); tmp++)
#define H_MAX_SANA 50
#define STR_END(str) (str)[strlen(str)-1]
#define CSANAT_DIR "/home/antterkk/csanat"
#define HOME "/home/antterkk/"
